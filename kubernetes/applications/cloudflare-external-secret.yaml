---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudflare-external-secret
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/toof-jp/infra
    targetRevision: HEAD
    path: kubernetes/applications/cloudflare-external-secret
  destination:
    server: https://kubernetes.default.svc
    namespace: cloudflare
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
